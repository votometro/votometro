# ============================================================================
# Votomatic Environment Variables
# ============================================================================
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#
# IMPORTANT: Also create /sanity/.env with Studio-specific variables (see bottom)
# ============================================================================

# ----------------------------------------------------------------------------
# Site Configuration
# ----------------------------------------------------------------------------
# Public URL of your site - used to generate magic links in emails
SITE_URL=http://localhost:4321

# ----------------------------------------------------------------------------
# Resend Email Service
# ----------------------------------------------------------------------------
# Get API key from: https://resend.com/api-keys
# Free tier: 100 emails/day, 3,000/month
RESEND_API_KEY=

# Verified sender email address (must verify domain in Resend)
RESEND_FROM_EMAIL=

# ----------------------------------------------------------------------------
# Sanity Webhook Security
# ----------------------------------------------------------------------------
# Secret for signing Sanity Studio → Frontend API requests (HMAC SHA-256)
# Generate with: openssl rand -hex 32
# MUST be identical in both root .env and /sanity/.env
SANITY_WEBHOOK_SECRET=

# ============================================================================
# QUICK SETUP GUIDE
# ============================================================================
#
# 1. Copy this file:
#      cp .env.example .env
#
# 2. Generate secrets:
#      openssl rand -hex 32     # For SANITY_WEBHOOK_SECRET
#
# 3. Sign up for Resend (https://resend.com):
#      - Get API key from: https://resend.com/api-keys
#      - Verify your domain: https://resend.com/domains
#      - Add RESEND_API_KEY and RESEND_FROM_EMAIL above
#
# 4. Create /sanity/.env:
#      SANITY_STUDIO_API_URL=http://localhost:4321
#      SANITY_STUDIO_WEBHOOK_SECRET=<copy-from-root-env>
#
# 5. Start development:
#      Terminal 1: cd frontend && pnpm dev
#      Terminal 2: cd sanity && pnpm dev
#
# ============================================================================
# PRODUCTION DEPLOYMENT (Cloudflare Pages)
# ============================================================================
#
# - Add ALL variables above as environment secrets in Cloudflare Pages
# - Update SITE_URL to production domain
# - Update SANITY_STUDIO_API_URL in Sanity Studio .env to production domain
# - Redeploy Sanity Studio: cd sanity && pnpm deploy
#
# ============================================================================
